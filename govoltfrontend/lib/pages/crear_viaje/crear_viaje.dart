import 'package:flutter/material.dart';

class CrearViajeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Crear Viaje'),
      ),
      body: Center(
        child: Text('This is the "Crear Viaje" screen.'),
      ),
    );
  }
}